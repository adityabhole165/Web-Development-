Practice Qustion 
Q1    A "good string " is a string that starts with the better 'a' and has a length > 3 . Write a program to find if a string is good or not 


    concept - 1st letter - a 
            lenght is greter than 3


Q2 . Predict the output of following code
let num = 12 ;
if((num % 3 === 0 ) && (num+1 == 15) || (num - 1 == 11) ){
    console.log("safe");
} else {
    console.log("unsafe");
}
